name: Build and Release

on:
  push:
    tags:
      - 'v*'  # Эта настройка будет срабатывать, когда пушится тег, начинающийся с "v"

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: '1.18'

      - name: Install dependencies
        run: go mod tidy

      - name: Build Go program
        run: go build -o my_program.exe

      - name: Archive templates and styles using PowerShell
        run: |
          Compress-Archive -Path templates, styles -DestinationPath release_files.zip

      - name: Create GitHub release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            my_program.exe
            release_files.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
